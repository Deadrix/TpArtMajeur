{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-success rounded-pill text-white fw-bold mt-3">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}